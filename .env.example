# OpenKeyHub 配置（复制为 .env 后按需修改；全部可留默认）
# 监听地址：0.0.0.0 = 局域网内都能访问（校内网部署用这个）
OKH_HOST=0.0.0.0
OKH_PORT=8011

# 上游厂家超时（秒）。大模型较慢时调大 READ。
OKH_CONNECT_TIMEOUT=20
OKH_READ_TIMEOUT=300

# 会话签名密钥：留空会自动生成并存到 data/secret.key。
# 多实例/想固定时可在此显式指定一段随机串。
# OKH_SECRET=
